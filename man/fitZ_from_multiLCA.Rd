% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lca_measurement.R
\name{fitZ_from_multiLCA}
\alias{fitZ_from_multiLCA}
\title{Estimate two-step covariate model via multilevLCA (optional reference path)}
\usage{
fitZ_from_multiLCA(
  data,
  Y.names,
  n_classes,
  Zp.names,
  maxIter.measurement,
  measurement.tol,
  covariate.tol,
  iter.measurement,
  R2.threshold,
  incomplete = FALSE,
  rebase = "C1",
  verbose = FALSE
)
}
\arguments{
\item{data}{A data.frame.}

\item{Y.names}{Character vector of item column names.}

\item{n_classes}{Integer. Number of latent classes.}

\item{Zp.names}{Character vector of covariate column names.}

\item{maxIter.measurement}{Maximum EM iterations.}

\item{measurement.tol}{Convergence tolerance.}

\item{covariate.tol}{NR tolerance for the covariate model.}

\item{iter.measurement}{Number of random restarts.}

\item{R2.threshold}{Entropy R\eqn{^2} restart threshold.}

\item{incomplete}{Logical. FIML for partially missing indicators. See the
\code{Missing Data} section of \code{vignette("tseLCA", package = "tseLCA")}.
Default \code{FALSE}.}

\item{rebase}{Character or integer. Reference class for column naming of
\verb{$mGamma}. Must match the \code{rebase} used in \code{\link[=three_step]{three_step()}} so
coefficient labels are consistent. Default \code{"C1"}.}

\item{verbose}{Logical.}
}
\value{
A list with the following elements:
\describe{
\item{\code{mGamma}}{Q x (T-1) numeric matrix of multinomial logit
coefficients. Rows are named by covariate (including \code{"Intercept"}),
columns by non-reference class (e.g. \code{"C2"}, \code{"C3"}).}
\item{\code{mPhi}}{Item parameter matrix (items x classes) from the
fixed-parameter multilevLCA fit.}
\item{\code{vOmega}}{Length-T vector of marginal class proportions, computed
as the average of the fitted class probability matrix (\code{vPi_avg} in
multilevLCA output).}
\item{\code{LLKSeries}}{Matrix of observed-data log-likelihoods across EM
iterations, passed through directly from the multilevLCA fit.}
\item{\code{raw_fit}}{The full \code{\link[multilevLCA:multiLCA]{multilevLCA::multiLCA()}} output object,
including \verb{$Varmat_cor} (corrected variance matrix) and
\verb{$SEs_cor_gamma} (corrected standard errors for \code{mGamma}) if
available.}
}
}
\description{
Calls \code{multilevLCA::multiLCA} with \code{fixedpars = 1} and \code{Z = Zp.names} to
fit the two-step covariate model.  This is the original multilevLCA approach
and is used when \code{get.twostep.vcov = TRUE} in \code{\link[=three_step]{three_step()}} to obtain
multilevLCA's corrected standard errors for the two-step gamma estimates.
}
\examples{
\donttest{
d <- generate_data(200, "high", "covariate", seed = 1)

# Two-step estimation via multiLCA (fixedpars = 1)
fZ_ml <- fitZ_from_multiLCA(
  data                = d,
  Y.names             = paste0("Y", 1:6),
  n_classes           = 3,
  Zp.names            = "Zp",
  maxIter.measurement = 5000L,
  measurement.tol     = 1e-8,
  covariate.tol       = 1e-6,
  iter.measurement    = 10L,
  R2.threshold        = 0.70
)
fZ_ml$mGamma           # two-step estimates
fZ_ml$raw_fit$Varmat_cor   # multilevLCA corrected vcov
}
}
