% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lca_measurement.R
\name{lca_step1}
\alias{lca_step1}
\title{Fit the LCA measurement model (Step 1)}
\usage{
lca_step1(
  data,
  Y.names,
  n_classes,
  Zp.names = NULL,
  maxIter.measurement = 5000L,
  measurement.tol = 1e-08,
  covariate.tol = 1e-06,
  iter.measurement = 10L,
  R2.threshold = 0.7,
  use.two.step = TRUE,
  estimate.one.step = TRUE,
  incomplete = FALSE,
  maxIter.fitZ = 200L,
  include.intercept = TRUE,
  rebase = "C1",
  startval = NULL,
  n_init = NULL,
  verbose = FALSE
)
}
\arguments{
\item{data}{A data.frame containing at minimum the indicator columns.}

\item{Y.names}{Character vector of item column names.}

\item{n_classes}{Integer. Number of latent classes.}

\item{Zp.names}{Character vector of covariate column names, or \code{NULL}.}

\item{maxIter.measurement}{Maximum EM iterations before giving up on convergence. Default \code{5000L}.}

\item{measurement.tol}{Convergence tolerance. Default \code{1e-8}.}

\item{covariate.tol}{Convergence tolerance for the \code{fitZ} M-step. Default \code{1e-6}.}

\item{iter.measurement}{Number of random restarts when entropy R\eqn{^2} is low. Default \code{10}.}

\item{R2.threshold}{Entropy R\eqn{^2} below which restarts are triggered. Default \code{0.7}.}

\item{use.two.step}{Logical. If \code{TRUE}, also estimate \code{fitZ} with \code{fitZ_from_fit0()} if \code{Zp.names} is applied. Default \code{TRUE}.}

\item{estimate.one.step}{Logical. If \code{FALSE}, skip the unconditional EM and only compute \code{fitZ}. Default \code{TRUE}.}

\item{incomplete}{Logical. FIML for partially missing indicators. See the
\code{Missing Data} section of \code{vignette("tseLCA", package = "tseLCA")}. Default \code{FALSE}.}

\item{maxIter.fitZ}{Maximum EM iterations for \code{fitZ_from_fit0()}. Default \code{200}.}

\item{include.intercept}{Logical. Prepend intercept to covariate design matrix. Default \code{TRUE}.}

\item{rebase}{Character or integer specifying the reference latent class.
Use \code{"C1"}, \code{"C2"}, etc. or an integer index. Default \code{"C1"}. The
measurement model is permuted so this class becomes column 1, making it
the reference for all downstream multinomial logit parameterizations.}

\item{startval}{Optional starting classification for the Step-1
measurement model: either an integer vector of length \code{nrow(data)}
(\verb{1..n_classes} per row) or a numeric matrix of conditional
item-response probabilities from which a classification is derived. See
\code{\link[=lca_step1_startval]{lca_step1_startval()}} for the full description of both forms. When
supplied, \code{lca_step1()} fits the measurement model with
\code{\link[=lca_step1_startval]{lca_step1_startval()}} instead of multilevLCA's default
k-means-on-principal-components initialization, and \code{estimate.one.step},
\code{iter.measurement}, and \code{R2.threshold} (which govern the default
restart-on-low-entropy behavior) are ignored. Mutually exclusive with
\code{n_init}. Default \code{NULL}.}

\item{n_init}{Optional positive integer. If supplied, fits the
measurement model \code{n_init} times from independent uniform-random
classifications (each through \code{startval}-style injection with
\code{kmea = FALSE}, not multilevLCA's k-means-on-PCA path) and keeps the
fit with the highest log-likelihood -- the unconditional multi-start
analog of \code{n_init} in \pkg{StepMix} or \code{nrep} in \pkg{poLCA}. Unlike
\code{iter.measurement} (which reruns multilevLCA's own k-means
initialization, and only when entropy R\eqn{^2} is low), all \code{n_init}
fits are always run. \code{estimate.one.step}, \code{iter.measurement}, and
\code{R2.threshold} are ignored when \code{n_init} is supplied. Mutually exclusive
with \code{startval}. Default \code{NULL}.}

\item{verbose}{Logical. Print progress messages. Default \code{FALSE}.}
}
\value{
A list with \verb{$fit0} (\code{\link[multilevLCA:multiLCA]{multilevLCA::multiLCA()}} measurement model) and \verb{$fitZ}
(two-step covariate model from \code{\link[=fitZ_from_fit0]{fitZ_from_fit0()}}, or \code{NULL}).
}
\description{
Estimates the latent class measurement model with \pkg{multilevLCA} and
optionally, fixes \code{mPhi} and estimates covariate effects (two-step
initialization) with \code{fitZ_from_fit0()}.
}
\examples{
\donttest{
d <- generate_data(200, "high", "covariate", seed = 1)

# Measurement model only
s1 <- lca_step1(d, Y.names = paste0("Y", 1:6), n_classes = 3)
s1$fit0$vPi    # estimated class prevalences
s1$fit0$mPhi   # item-response probabilities

# With two-step covariate initialization
s1z <- lca_step1(d, Y.names = paste0("Y", 1:6), n_classes = 3,
                 Zp.names = "Zp", use.two.step = TRUE, verbose = TRUE)
s1z$fitZ$mGamma   # two-step gamma estimates

# Many random-classification restarts, keeping the best (analogous to
# n_init in StepMix or nrep in poLCA)
s1r <- lca_step1(d, Y.names = paste0("Y", 1:6), n_classes = 3,
                 n_init = 20L, verbose = TRUE)
}
}
