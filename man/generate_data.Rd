% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dgp.R
\name{generate_data}
\alias{generate_data}
\title{Generate one dataset following the Bakk & Kuha (2018) simulation design}
\usage{
generate_data(
  n,
  separation = c("low", "mid", "high"),
  scenario = c("covariate", "distal"),
  params = bk2018_params,
  seed = NULL
)
}
\arguments{
\item{n}{Integer. Sample size (paper uses 500, 1000, or 2000).}

\item{separation}{Character. One of \code{"low"}, \code{"mid"}, \code{"high"}.
Maps to pi = 0.70, 0.80, 0.90 respectively.}

\item{scenario}{Character. One of:
\describe{
\item{\code{"covariate"}}{Zp (discrete, 1-5) predicts latent X via multinomial logit.}
\item{\code{"distal"}}{Latent X predicts continuous Zo via linear regression.}
}}

\item{params}{List of population parameters.  Defaults to \link{bk2018_params}.}

\item{seed}{Integer or \code{NULL}. Optional random seed for reproducibility.}
}
\value{
A \code{data.frame} with columns:
\describe{
\item{\code{Y1} .. \code{Y6}}{Binary indicators (always present).}
\item{\code{X}}{True latent class, integer 1-3 (not observed in practice).}
\item{\code{Zp}}{Integer covariate 1-5 (scenario \code{"covariate"} only).}
\item{\code{Zo}}{Continuous distal outcome (scenario \code{"distal"} only).}
}
}
\description{
Generate one dataset following the Bakk & Kuha (2018) simulation design
}
\examples{
# Covariate scenario with high separation
d <- generate_data(n = 200, separation = "high", scenario = "covariate",
                   seed = 1)
head(d)
colMeans(d)

# Distal outcome scenario
d2 <- generate_data(n = 200, separation = "high", scenario = "distal",
                    seed = 2)
head(d2)
}
