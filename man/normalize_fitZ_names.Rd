% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean.R
\name{normalize_fitZ_names}
\alias{normalize_fitZ_names}
\title{Normalize row/column names of a fitZ$mGamma matrix}
\usage{
normalize_fitZ_names(fitZ, Zp.names = NULL, n_classes = NULL)
}
\arguments{
\item{fitZ}{A fitZ-like list with at least \verb{$mGamma}.}

\item{Zp.names}{Character vector of covariate column names (used to set
clean rownames when the raw names can't be parsed). If \code{NULL}, rownames
are stripped from the \code{gamma(X|C)} pattern only.}

\item{n_classes}{Integer. Total number of classes (used to derive clean
column names if they are non-standard).}
}
\value{
\code{fitZ} with normalized \verb{$mGamma} row/col names.
}
\description{
A plain \code{multiLCA} object uses \code{rownames} like \code{"gamma(Intercept|C)"} and
\code{"gamma(Zp|C)"}. This function strips the \code{gamma(...)} wrapper so names
match the clean format used throughout tseLCA (\code{"Intercept"}, \code{"Zp"}, etc.)
and ensures column names are \code{"C2"}, \code{"C3"}, etc.
}
\keyword{internal}
