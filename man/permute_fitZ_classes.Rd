% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean.R
\name{permute_fitZ_classes}
\alias{permute_fitZ_classes}
\title{Permute class columns of a fitZ object to match a new reference class}
\usage{
permute_fitZ_classes(fitZ, ref_idx)
}
\arguments{
\item{fitZ}{Output of \code{fitZ_from_fit0()} or \code{fitZ_from_multiLCA()}.}

\item{ref_idx}{Integer. New reference class (1-based index into the T
classes as currently ordered in \code{fitZ}).}
}
\value{
\code{fitZ} with \verb{$mGamma}, \verb{$Varmat_cor}, and names updated.
}
\description{
Rebases a \code{fitZ} object (output of \code{fitZ_from_fit0} or
\code{fitZ_from_multiLCA}) so that \code{ref_idx} becomes the reference class.
This involves:
\enumerate{
\item Rebasing \verb{$mGamma}: reconstructing the full T-column log-ratio
matrix, subtracting the new reference column, and dropping it.
\item Propagating through \verb{$Varmat_cor} via the delta method: the
rebasing transformation is linear (\code{gamma_new = A * gamma_old})
so the vcov transforms exactly as \code{A \%*\% V \%*\% t(A)}.
\item Updating all column names.
}
}
\keyword{internal}
