% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/three_step.R
\name{coef.tseLCA_measurement}
\alias{coef.tseLCA_measurement}
\alias{coef.tseLCA_covariate}
\alias{coef.tseLCA_distal}
\alias{coef.tseLCA_both}
\title{Extract coefficients from a tseLCA model object}
\usage{
\method{coef}{tseLCA_measurement}(object, ...)

\method{coef}{tseLCA_covariate}(object, which = c("three_step", "two_step"), ...)

\method{coef}{tseLCA_distal}(object, ...)

\method{coef}{tseLCA_both}(
  object,
  which = c("both", "covariate", "distal"),
  step = c("three_step", "two_step"),
  ...
)
}
\arguments{
\item{object}{A \code{tseLCA} object returned by \code{\link[=three_step]{three_step()}}.}

\item{...}{Further arguments (currently unused).}

\item{which}{Character. For covariate and both models: \code{"three_step"}
(default) or \code{"two_step"}. For both models also accepts \code{"covariate"},
\code{"distal"}, or \code{"both"}.}

\item{step}{Character. For \code{tseLCA_both}: \code{"three_step"} (default) or
\code{"two_step"}.}
}
\value{
The coefficient matrix (covariate models), named numeric vector
(distal models), or a named list of both (measurement or both models).
}
\description{
Extract coefficients from a tseLCA model object
}
\examples{
d    <- generate_data(100, "high", "covariate", seed = 1)
fit_m <- three_step(d, paste0("Y", 1:6), n_classes = 3)
coef(fit_m)   # returns list with $prevalences and $item_probs
\donttest{
d   <- generate_data(200, "high", "covariate", seed = 1)
fit <- three_step(d, paste0("Y", 1:6), n_classes = 3,
                  Zp.names = "Zp", use.simple.cov = TRUE)
coef(fit)                      # three-step estimates
coef(fit, which = "two_step")  # two-step starting values
}
\donttest{
d   <- generate_data(200, "high", "distal", seed = 2)
fit <- three_step(d, paste0("Y", 1:6), n_classes = 3,
                  Zo.name = "Zo", use.simple.cov = TRUE)
coef(fit)   # named vector of class means
}
\donttest{
d   <- generate_data(200, "high", "covariate", seed = 1)
d$Zo <- rnorm(200, mean = c(-1, 0, 1)[d$X], sd = 1)
fit <- three_step(d, paste0("Y", 1:6), n_classes = 3,
                  Zp.names = "Zp", Zo.name = "Zo",
                  use.simple.cov = TRUE)
coef(fit, which = "covariate")
coef(fit, which = "distal")
coef(fit, which = "both")
}
}
