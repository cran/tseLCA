% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dgp.R
\name{generate_all_conditions}
\alias{generate_all_conditions}
\title{Generate datasets for all 18 conditions in the simulation design}
\usage{
generate_all_conditions(
  n_rep = 500L,
  base_seed = 5262026L,
  params = bk2018_params,
  scenarios = c("covariate", "distal"),
  sep_levels = c("low", "mid", "high"),
  sample_sizes = c(500L, 1000L, 2000L),
  verbose = TRUE
)
}
\arguments{
\item{n_rep}{Integer. Replications per condition (paper uses 500).}

\item{base_seed}{Integer. Base seed for reproducibility.}

\item{params}{Population parameters list.  Defaults to \link{bk2018_params}.}

\item{scenarios}{Character. Lists the scenario(s) ("covariate" and/or "distal")
wanting to be simulated. Passed into \code{\link[=generate_data]{generate_data()}}.}

\item{sep_levels}{Character. Lists the separation level(s) ("low", "mid", "high")
wanting to be simulated. Passed into \code{\link[=generate_data]{generate_data()}}.}

\item{sample_sizes}{Integer. Lists the sample size(s) wanting to be generated
for each replication condition. Passed into \code{\link[=generate_data]{generate_data()}}.}

\item{verbose}{Logical. If \code{TRUE} (default), display a live CLI progress
bar with per-rep status and ETA.}
}
\value{
Nested list indexed as
\code{datasets[[scenario]][[separation]][[as.character(n)]]},
each element a list of \code{n_rep} data frames.
}
\description{
Iterates over the 2 scenarios x 3 separation levels x 3 sample sizes,
generating \code{n_rep} independent replications per condition.  Seeds are
derived deterministically from \code{base_seed} so the entire experiment is
reproducible from a single integer.
}
\examples{
\donttest{
# Generate 5 replicates for mid and high separation only
datasets <- generate_all_conditions(n_rep = 5L, base_seed = 1L,
                                    sep_levels = c("mid", "high"))
# Access a single replicate
head(datasets[["covariate"]][["high"]][["500"]][[1]])
}
}
